# Workspace monitor assignments
# Only applied when both monitors are connected.
# If a monitor is absent, Hyprland silently ignores the rule and
# workspaces fall back to wherever Hyprland places them.
